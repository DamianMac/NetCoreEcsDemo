{
  "ConnectionString": "host=localhost;database=products;username=product_user;password=verysecret;",

  "Serilog": {
    "WriteTo": [{
      "Name": "Seq",
      "Args": {
        "serverUrl": "http://localhost:5341"
      }
    }]
  }
}
